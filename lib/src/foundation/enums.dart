/// The alignment of [ProgressBar] relative to it's total size.
enum ProgressBarAlignment { center, bottom }

/// The shape of the bars at the left and right edges.
enum BarCapShape { round, square }
